-- =============================================================================
-- 修正 stock_lots 的資料字典註解。
--
-- 唯一鍵 ux_stock_lots_natural_key 是 (item_id, lot_number, storage_location)；
-- expiry_date 不在唯一鍵內。既有批次遇到不同效期時由入庫流程明確拒絕。
-- =============================================================================

COMMENT ON TABLE stock_lots IS '庫存批次。品項+批號+儲位 的唯一組合（效期不在唯一鍵內：同一批號只有一個效期，入庫時效期不同會被拒絕）；數量掛在這裡。';
