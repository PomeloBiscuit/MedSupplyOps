# Windows/.NET 專案：repo 內存 LF，工作目錄依平台（Windows 得到 CRLF）。
# 與 .editorconfig 的 end_of_line=crlf 一致。
* text=auto
*.sln  text eol=crlf
*.slnx text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf
*.png binary
*.jpg binary
*.ico binary
docs/diagrams/*.svg text eol=lf

# 在容器裡（Linux）執行的檔案一律 LF。
# 這些檔案在 Windows 上被 checkout 成 CRLF 的話，會在容器內爆
# `$'': command not found` —— 而錯誤訊息完全不會提到換行字元。
*.sh text eol=lf
db/init/** text eol=lf
