# ── 本機參考資料，不是交付物 ─────────────────────────────

# ── .NET ────────────────────────────────────────────────
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
TestResults/
*.trx
*.coverage
coverage*.json
coverage*.xml
*.received.*

# ── 機敏設定：連線字串一律走 User Secrets / 環境變數 ──────────
appsettings.Development.json
appsettings.Local.json
secrets.json
*.env

# ── Oracle wallet / 憑證 ────────────────────────────────
wallet/
*.pfx
*.p12

# ── OS / Editor ─────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp

# Docker 環境變數（含密碼）
.env

# 本機工作筆記：不是交付物的一部分。
_private/

# 維運備份含業務資料與 Identity 密碼雜湊，絕不可納入版本控制。
backups/

# Claude Code 的本機狀態：工作樹複本裡含 .env（資料庫密碼），絕不可進版本庫。
.claude/
